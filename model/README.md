# model/

Place your trained ONNX model here:

    model/best.onnx

The file is referenced in app.py as:
    MODEL_PATH = os.path.join(os.path.dirname(__file__), "model", "best.onnx")

Your current model path from the screenshot:
    OneDrive/Sricharan - Personal/Desktop/pill_counter_final/PillDetection/pill_model_run/weights/best.onnx

Copy best.onnx into this folder before running the app.
